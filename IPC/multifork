#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include<unistd.h>
#include<time.h>
#include <sys/wait.h>
int main(int argc,char* argv[]){
        int id = fork();
 	int id2 = fork();
	if(id == 0){	
		if(id2 == 0){
			printf("i'M CHILD'S child");
		}
		else{
			printf("Child but parent");
		}
	else{
		printf("Parent");
	}

        return 0;
}
